apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonAnnotations:
  linkerd.io/inject: enabled

resources:
  - backups.yaml
  - hello-world.yaml
  - registry-nfs.yml
  - registry.yml
  - media-nfs.yml
  - media-store.yaml
  - cd.yaml
